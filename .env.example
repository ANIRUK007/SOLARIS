# Copy to .env and fill in. .env is gitignored — never commit real keys.
#
# The previous version of this app shipped both keys inside index.html, which
# put them in the public repository and in its history. Any key that was ever
# committed there must be treated as compromised and rotated at the provider.

PORT=3001

# Where recordings are written. Relative paths resolve next to server.js.
SOLARIS_DATASET_DIR=./dataset

# Speech-to-text. Leave a key blank to disable that engine; the app then
# falls back to manual transcription instead of failing.
SARVAM_API_KEY=
SARVAM_MODEL=saarika:v2.5

GROQ_API_KEY=
GROQ_MODEL=whisper-large-v3-turbo

# HTTPS, required before phones will grant microphone access over the LAN.
# Run `npm run certs` to generate these.
# SSL_CERT=./certs/cert.pem
# SSL_KEY=./certs/key.pem

# ── Database ─────────────────────────────────────────────────────────────────
# With these set, Supabase is the source of truth. Without them the server runs
# on JSON files next to itself, which is what a field laptop with no uplink
# uses. Both are in the Supabase dashboard under Project Settings → API.
#
# Use the service_role key, not the anon key: these tables have row-level
# security enabled with no permissive policy, so only the service role can read
# or write them. That key must never reach a browser.
#
# Apply db/schema.sql first, then: npm run migrate
# SUPABASE_URL=https://yourproject.supabase.co
# SUPABASE_SERVICE_KEY=

# ── Access ───────────────────────────────────────────────────────────────────
# Open sign-up is the default. Set to false for a closed study where a lead
# creates every account.
# SOLARIS_OPEN_REGISTRATION=false
# Or keep it open only to people who were given a code.
# SOLARIS_INVITE_CODE=
# Origins allowed to call the API from a browser. Empty means same-origin only,
# which is all this app needs.
# SOLARIS_ALLOWED_ORIGINS=
# Set only when actually behind a reverse proxy, or anyone can spoof their
# address and get a fresh rate-limit quota per request.
# SOLARIS_TRUST_PROXY=true
# The timezone that decides when a day ends, for streaks.
# SOLARIS_TIMEZONE=Asia/Kolkata
