# Secrets — the whole point of moving the API keys server-side.
.env

# Self-signed certificates, machine-specific.
certs/

# Collected audio. The dataset does not belong in the code repository.
dataset/

node_modules/
npm-debug.log*
.DS_Store
Thumbs.db

# Generated by the end-to-end run.
tests/screenshots/

# Account store: password hashes and the token-signing secret.
.solaris-users.json
.solaris-users.json.*

# Who has recorded what. Derived data — rebuildable from the recordings.
.solaris-words.json
.solaris-words.json.*
